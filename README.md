# Scoop bucket for LoTaR

This repository hosts the Scoop manifest(s) for the `lotar` CLI. Each tag update publishes a fresh `bucket/lotar.json` file that points at the signed Windows release uploaded from the main repository.
